# 快速开始

欢迎使用 PolarisDesk！这份指南将帮助你快速上手。

## 下载安装

### 下载应用

访问 [GitHub Releases](https://github.com/t8y2/PolarisDesk/releases) 下载最新版本：

- **Windows**: `.exe` 安装包
- **macOS**: `.dmg` 文件
- **Linux**: `.AppImage` 文件

### 安装步骤

详细的安装说明请查看 [安装指南](./installation.md)。

## 首次配置

### 1. 选择 AI 服务商

首次启动后，需要配置 AI 服务：

1. 点击右上角的 **设置** 图标 ⚙️
2. 在 **模型设置** 中选择 AI 服务商
3. 常用服务商：
   - **OpenAI** - GPT-4, GPT-4o
   - **Anthropic** - Claude 3
   - **DeepSeek** - 性价比高
   - **Ollama** - 本地运行，保护隐私

### 2. 配置 API Key

1. 点击服务商旁边的 **获取 Key** 链接
2. 访问服务商官网注册并获取 API Key
3. 将 API Key 粘贴到输入框
4. 点击 **保存**

::: tip 提示
API Key 存储在本地，不会上传到云端。
:::

### 3. 选择模型

根据你的需求选择合适的模型：

- **日常对话**: GPT-3.5-Turbo, DeepSeek-Chat
- **复杂任务**: GPT-4, Claude 3 Opus
- **代码编写**: DeepSeek-Coder, GPT-4
- **图片分析**: GPT-4o, Claude 3, Gemini Pro Vision

## 开始使用

### 基本对话

1. 在输入框中输入你的问题
2. 按 `Enter` 发送（`Shift+Enter` 换行）
3. AI 将实时返回回答

**示例问题**:

```
解释一下 JavaScript 的闭包是什么？
```

### 上传图片

1. 点击输入框左侧的 📎 图标
2. 选择图片文件
3. 输入问题，如"这张图片里有什么？"
4. 发送消息

::: warning 注意
需要使用支持视觉的模型（GPT-4o, Claude 3, Gemini Pro Vision）。
:::

### 截图分析

1. 点击输入框左侧的 📸 图标（或按 `Ctrl/Cmd+Shift+A`）
2. 截图会自动添加到输入框
3. 输入问题，如"这段代码有什么问题？"
4. 发送消息

### 使用人设预设

1. 点击输入框左侧的 🎭 图标
2. 选择合适的人设（如"编程专家"）
3. 开始对话，AI 会以该角色回答

### AI 命令行

直接用自然语言描述想执行的操作：

```
列出当前目录下所有的 Python 文件
```

AI 会生成对应的命令，你可以选择执行。

## 常用功能

### 新建对话

- 快捷键: `Ctrl/Cmd + Shift + N`
- 或点击左上角的 ➕ 按钮

### 查看历史

- 快捷键: `Ctrl/Cmd + Shift + H`
- 或点击顶部的历史记录图标

### 悬浮窗口

- 快捷键: `Ctrl/Cmd + Shift + Space`
- 或点击顶部的悬浮窗图标
- 悬浮窗可以在任何应用上方使用

### 隐私模式

- 点击顶部的隐私模式图标 🔒
- 开启后，对话不会保存到历史记录

## 快捷键

| 功能       | Windows/Linux      | macOS             |
| ---------- | ------------------ | ----------------- |
| 发送消息   | `Enter`            | `Enter`           |
| 换行       | `Shift+Enter`      | `Shift+Enter`     |
| 新建对话   | `Ctrl+Shift+N`     | `Cmd+Shift+N`     |
| 打开历史   | `Ctrl+Shift+H`     | `Cmd+Shift+H`     |
| 快速截图   | `Ctrl+Shift+A`     | `Cmd+Shift+A`     |
| 区域截图   | `Ctrl+Shift+X`     | `Cmd+Shift+X`     |
| 切换悬浮窗 | `Ctrl+Shift+Space` | `Cmd+Shift+Space` |
| 粘贴图片   | `Ctrl+V`           | `Cmd+V`           |

## 进阶使用

### 调整模型参数

在设置中可以调整：

- **Temperature**: 控制回复的创造性（0.0-2.0）
  - 低值（0.0-0.3）: 更准确、一致
  - 中值（0.5-0.8）: 平衡
  - 高值（0.8-1.2）: 更有创意

- **Max Tokens**: 控制回复的最大长度
  - 简短回答: 1000-2000
  - 正常对话: 2000-4000
  - 长文本: 4000-8000

- **历史轮数**: 控制发送给 AI 的历史对话数量
  - 0: 不携带历史
  - 10-20: 正常对话
  - 20+: 长期对话

### 自定义系统提示词

在设置中可以自定义系统提示词，定义 AI 的行为：

```
你是一位专业的助手，请用简洁明了的方式回答问题。
```

### 创建自定义人设

1. 打开 **设置** > **人设预设**
2. 点击 **添加人设**
3. 填写名称、描述和提示词
4. 保存并使用

## 开发者模式

如果你想从源码运行或参与开发：

### 环境要求

- Node.js >= 18
- pnpm >= 8
- macOS: Xcode Command Line Tools
- Windows: Visual Studio Build Tools

### 安装依赖

```bash
git clone https://github.com/t8y2/PolarisDesk.git
cd PolarisDesk
pnpm install
```

### 启动开发服务器

```bash
pnpm dev
```

### 构建应用

```bash
# 构建当前平台
pnpm build

# 构建 Windows
pnpm build:win

# 构建 macOS
pnpm build:mac

# 构建 Linux
pnpm build:linux
```

## 获取帮助

### 文档

- [功能介绍](../features/ai-chat.md)
- [安装指南](./installation.md)
- [常见问题](#)

### 社区

- [GitHub Issues](https://github.com/t8y2/PolarisDesk/issues) - 报告问题
- [Discord](https://discord.gg/6XR6b73PrE) - 社区讨论
- 微信群 - 扫描 README 中的二维码

## 下一步

- 探索 [AI 对话](../features/ai-chat.md) 的高级功能
- 了解 [人设预设](../features/persona-presets.md) 的使用
- 尝试 [AI 命令行](../features/ai-command.md) 提升效率
- 查看 [设置](../features/settings.md) 自定义应用

祝你使用愉快！🚀
